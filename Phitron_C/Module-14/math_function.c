#include<stdio.h>
#include<math.h>
#include<stdlib.h>

int main()
{
    // double n;
    // scanf("%lf",&n);
    // int ans=ceil(n);
    // int ans=floor(n);
    // int ans=round(n);
    // printf("%d",ans);

    // double x;
    // scanf("%lf",&x);
    // double ans=sqrt(x);
    // printf("%.2lf",ans);

    // int y,z;
    // scanf("%d%d",&y,&z);
    // double ans=pow(y,z);
    // printf("%.0lf",ans);

    int a;
    scanf("%d",&a);
    int ans=abs(a);
    printf("%d",ans);

    return 0;
}